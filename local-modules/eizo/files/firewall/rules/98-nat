# -*- mode: sh; -*-

# Default NAT rule
[[ -z $ip[firewall] ]] || \
    for iface in $lans; do
        iptables -t nat -A POSTROUTING -i $iface -o internet -j SNAT --to-source ${ip[firewall]}
    done
